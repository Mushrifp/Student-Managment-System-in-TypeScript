<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Portal</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Söhne, -apple-system, BlinkMacSystemFont, Helvetica, sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #19193d, #040409);
            padding: 20px;
        }
        .container {
            width: 100%;
            max-width: 900px;
            display: flex;
            flex-direction: column;
            background: linear-gradient(135deg, #19193d, #040409);
            backdrop-filter: blur(15px);
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }
        h1 {
            font-size: 28px;
            font-weight: 600;
            text-align: center;
            margin-bottom: 10px;
            color: rgba(255, 255, 255, 0.9);
        }
        .form-section {
            flex: 1;
            padding: 40px;
            text-align: center;
        }
        .welcome-text {
            font-size: 16px;
            color: #b3b3b3;
            margin-bottom: 20px;
        }
        .form-group {
            margin-bottom: 25px;
            text-align: left;
        }
        .input-label {
            color: rgba(255, 255, 255, 0.7);
            font-weight: 500;
            display: block;
            margin-bottom: 8px;
        }
        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 14px;
            font-size: 16px;
            color: #fff;
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.08);
            outline: none;
            transition: all 0.3s;
        }
        input[type="email"]::placeholder,
        input[type="password"]::placeholder {
            color: rgba(255, 255, 255, 0.4);
        }
        input[type="email"]:focus,
        input[type="password"]:focus {
            border-color: rgba(255, 255, 255, 0.6);
            box-shadow: 0 0 8px rgba(255, 255, 255, 0.2);
        }
        .error-message {
            font-size: 15px;
            color: rgba(255, 85, 85, 0.9);
            margin-top: 6px;
        }
        button {
            width: 100%;
            padding: 14px;
            font-size: 18px;
            font-weight: 600;
            color: #fff;
            background: linear-gradient(135deg, #21d4fd, #2152ff);
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s;
        }
        button:hover {
            background: linear-gradient(135deg, #1a9cf8, #1e3dbb);
        }
        .divider {
            height: 2px;
            width: 80%;
            margin: 20px auto;
            background: rgba(255, 255, 255, 0.1);
        }
        @media (min-width: 768px) {
            .container {
                flex-direction: row;
                justify-content: space-between;
            }
            .form-section {
                padding: 40px 20px;
                width: 50%;
            }
            .divider {
                width: 2px;
                height: auto;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-section">
            <p class="welcome-text">Welcome back</p>
            <h1>Student Login</h1>
            <form action="/stdVerification" method="post">
                <div class="form-group">
                    <label class="input-label" for="student-email">Email address</label>
                    <input type="email" id="student-email" name="email" placeholder="Enter your email" value="<%= typeof stdData !== 'undefined' ? stdData.email : '' %>" required>
                </div>
                <div class="form-group">
                    <label class="input-label" for="student-password">Password</label>
                    <input type="password" id="student-password" name="password" placeholder="Enter your password" value="<%= typeof stdData !== 'undefined' ? stdData.password : '' %>" required>
                    <span class="error-message" id="student-error">
                        <% if (typeof stdEmailError !== 'undefined') { %>invalid email<% } else if (typeof stdPassError !== 'undefined') { %>invalid password<% } %>
                    </span>
                </div>
                <button type="submit">Sign in</button>
            </form>
        </div>
        
        <div class="divider"></div>
        
        <div class="form-section">
            <p class="welcome-text">Welcome back</p>
            <h1>Teacher Login</h1>
            <form action="/dashboard" method="post">
                <div class="form-group">
                    <label class="input-label" for="teacher-email">Email address</label>
                    <input type="email" id="teacher-email" name="email" placeholder="Enter your email" value="<%= typeof data !== 'undefined' ? data.email : '' %>" required>
                </div>
                <div class="form-group">
                    <label class="input-label" for="teacher-password">Password</label>
                    <input type="password" id="teacher-password" name="password" placeholder="Enter your password" value="<%= typeof data !== 'undefined' ? data.password : '' %>" required>
                    <span class="error-message">
                        <% if (typeof emailError !== 'undefined') { %>invalid email<% } else if (typeof passError !== 'undefined') { %>invalid password<% } %>
                    </span>
                </div>
                <button type="submit">Sign in</button>
            </form>
        </div>
    </div>
</body>
</html>
