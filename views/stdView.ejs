<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Details</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #19193d, #040409);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            color: #ececec;
        }

        .container {
            max-width: 600px;
            width: 100%;
            background: linear-gradient(135deg, #19193d, #040409);
            border-radius: 15px;
            padding: 40px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .container:hover {
            transform: translateY(-10px);
            box-shadow: 0 16px 30px rgba(0, 0, 0, 0.5);
        }

        h1 {
            text-align: center;
            font-size: 1.8em;
            margin-bottom: 30px;
            color: #ececec;
            font-weight: 600;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 1em;
            margin-bottom: 20px;
            padding: 10px;
            border-bottom: 1px solid #333344;
        }

        .label {
            font-weight: 500;
            color: #a5a5b0;
        }

        .value {
            color: #ececec;
            font-weight: 400;
        }

        .view-btn {
            display: block;
            width: 100%;
            text-align: center;
            background-color: #4a90e2;
            color: #fff;
            padding: 12px 0;
            border-radius: 8px;
            font-size: 1em;
            font-weight: 500;
            margin-top: 30px;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .view-btn:hover {
            background-color: #357ab8;
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }

            h1 {
                font-size: 1.6em;
            }

            .view-btn {
                font-size: 0.9em;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Student Details</h1>

        <div class="detail-item">
            <span class="label">Name:</span>
            <span class="value"><%= data[0].name %></span>
        </div>

        <div class="detail-item">
            <span class="label">Email:</span>
            <span class="value"><%= data[0].email %></span>
        </div>

        <div class="detail-item">
            <span class="label">Password:</span>
            <span class="value"><%= data[0].password %></span>
        </div>

        <div class="detail-item">
            <span class="label">Roll No:</span>
            <span class="value"><%= data[0].rollno %></span>
        </div>

        <div class="detail-item">
            <span class="label">Course:</span>
            <span class="value"><%= data[0].course %></span>
        </div>

    </div>
</body>
</html>
